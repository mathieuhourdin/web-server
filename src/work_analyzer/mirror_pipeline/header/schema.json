{
    "type": "object",
    "properties": {
        "title": { "type": "string" },
        "subtitle": { "type": "string" },
        "trace_mirror_type": {
            "type": "string",
            "enum": ["Bio", "Journal", "Note", "BIO", "JOURNAL", "NOTE"]
        },
        "tags": {
            "type": "array",
            "items": { "type": "string" }
        },
        "high_level_projects": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "id": { "type": "integer" },
                    "span": { "type": "string" }
                },
                "required": ["id", "span"],
                "additionalProperties": false
            }
        }
    },
    "required": ["title", "subtitle", "trace_mirror_type", "tags", "high_level_projects"],
    "additionalProperties": false
}
