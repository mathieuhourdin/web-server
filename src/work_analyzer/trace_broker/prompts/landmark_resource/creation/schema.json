{
    "type": "object",
    "properties": {
        "title": {"type": "string"},
        "author": {"type": "string"},
        "content": {"type": "string"},
        "identity_state": {"type": "string", "enum": ["identified", "stub", "discard"]}
    },
    "required": ["title", "author", "content", "identity_state"],
    "additionalProperties": false
}